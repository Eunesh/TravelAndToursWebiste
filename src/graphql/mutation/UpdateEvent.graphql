mutation UpdateEvent($id: ID!, $name: String, $description: String, $placeId: ID ) {
  updateEvent(
    id: $id
    name: $name
    description: $description
    placeId: $placeId
  ) {
    id
    name
    description
    bannerUrl
    pictureUrls
    place {
      id
      name
    }
  }
}